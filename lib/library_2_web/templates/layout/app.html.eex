<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Library</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <header>
      <section class="container" role="navigation">
        <div class="topnav">
          <%= link "Home", to: Routes.page_path(@conn, :index) %>
          <%= link "Authors", to: Routes.author_path(@conn, :index) %>
          <%= link "Categories", to: Routes.category_path(@conn, :index) %>
          <%= link "Books", to: Routes.book_path(@conn, :index) %>
          <%= link "Clients", to: Routes.client_path(@conn, :index) %>
        </div>
      </section>
    </header>
    <main role="main" class="container">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
  </body>
</html>
